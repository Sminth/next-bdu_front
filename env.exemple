# En dev local (npm run dev), pointe directement sur le backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
NEXT_PUBLIC_UNIVERSE=bdu
NEXT_PUBLIC_DOMAIN=localhost
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# URL interne du backend utilisée par le proxy Next.js (server-side uniquement)
# En dev local : http://localhost:3001
# En Docker   : http://backend:3001 (géré par docker-compose, ne pas changer ici)
INTERNAL_BACKEND_URL=http://localhost:3001
